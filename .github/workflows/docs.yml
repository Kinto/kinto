on:
  push:
    branches:
      - master
  pull_request:

name: Docs
jobs:
  chore:
    name: Validate docs
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: "3.x"

      - name: Print environment
        run: |
          python --version
          pip --version

      - name: make docs
        run: make docs
